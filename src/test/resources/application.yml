# jpa
spring:
  profiles:
    include: secret  # 민감정보 secret 파일
    
  jpa:
    properties:
      hibernate:
        default_batch_fetch_size: 100
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

  datasource:
    url: jdbc:mysql://sanrio-database.cv0zl8agiiz8.ap-northeast-2.rds.amazonaws.com:3306/mydb?serverTimezone=UTC&characterEncoding=UTF-8
    username: thumper
    password: h1049688
    driver-class-name: com.mysql.cj.jdbc.Driver

  # cloud
  cloud:
    gcp:
      storage:
        credentials:
          location: classpath:daily-commit-424306-fcc0c61d0682.json
        project-id: daily-commit-424306
        bucket: java-board-bucket

  jwt:
    secret: vmfhaltmskdlstkfkdgodyroqkfwkdbalroqkfwkdbalaaaaaaaaaaaaaaaabbbbb

  # swagger
  swagger-ui:
    groups-order: DESC
    tags-sorter: alpha
    operations-sorter: method
    disable-swagger-default-url: true
    display-request-duration: true
    defaultModelsExpandDepth: 2
    defaultModelExpandDepth: 2

  api-docs:
    path: /api-docs

  show-actuator: true
  writer-with-default-pretty-printer: true
  model-and-view-allowed: true
  paths-to-match:
    - /api/**

# log
logging:
  level:
    org.springframework.security: DEBUG